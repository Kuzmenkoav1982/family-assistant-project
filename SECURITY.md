# 🛡️ Документация по безопасности приложения "Наша Семья"

## Обзор

Приложение "Наша Семья" использует многоуровневую систему защиты данных, соответствующую требованиям **152-ФЗ "О персональных данных"** и стандартам банковской безопасности.

---

## 📋 Оглавление

1. [Архитектура безопасности](#архитектура-безопасности)
2. [Защита учетных записей](#защита-учетных-записей)
3. [Шифрование данных](#шифрование-данных)
4. [Аудит и мониторинг](#аудит-и-мониторинг)
5. [Политики и согласия](#политики-и-согласия)
6. [Для разработчиков](#для-разработчиков)
7. [Инциденты безопасности](#инциденты-безопасности)

---

## 🏗️ Архитектура безопасности

### Уровни защиты

```
┌─────────────────────────────────────────────────────────┐
│ УРОВЕНЬ 1: Защита от атак                               │
│ • Rate Limiting (5 попыток / 15 мин)                    │
│ • IP-tracking и блокировка                              │
│ • CORS-политики                                         │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│ УРОВЕНЬ 2: Аутентификация                               │
│ • bcrypt хеширование (12 раундов)                       │
│ • Токены сессий (30 дней)                               │
│ • OAuth через Яндекс ID                                 │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│ УРОВЕНЬ 3: Шифрование данных                            │
│ • AES-256-GCM для медицинских записей                   │
│ • HTTPS для всех соединений                             │
│ • Секреты в защищенном хранилище                        │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│ УРОВЕНЬ 4: Аудит и мониторинг                           │
│ • Логирование всех критичных действий                   │
│ • Автоматическая очистка старых данных                  │
│ • Мониторинг подозрительной активности                  │
└─────────────────────────────────────────────────────────┘
```

---

## 🔐 Защита учетных записей

### 1. Хеширование паролей (bcrypt)

**Что это значит для вас:**
- Даже администраторы не могут увидеть ваш пароль
- При утечке базы данных пароли невозможно восстановить
- Каждый пароль хешируется уникальным способом (соль)

**Технические детали:**
```python
# Хеширование
bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt(rounds=12))
# Результат: $2b$12$kBAfIqu.p53VYPG2oQgFXe4iVp0w93rVj3i4CGpEpTqRZSn3lfGvi

# Время взлома одного пароля: ~10 лет (на современном оборудовании)
```

### 2. Rate Limiting (защита от брутфорса)

**Лимиты по типам операций:**

| Операция | Лимит | Период | Действие при превышении |
|----------|-------|--------|------------------------|
| Вход в систему | 5 попыток | 15 минут | Блокировка на 15 минут |
| Восстановление пароля | 3 попытки | 30 минут | Блокировка на 30 минут |
| Регистрация | 5 попыток | 15 минут | Блокировка на 15 минут |

**Что видит пользователь:**
```
⚠️ Осталось попыток: 2

🔒 Вход заблокирован. Попробуйте через 12 минут
```

**Как это работает:**
1. При каждой попытке входа система проверяет ваш IP-адрес
2. Если попыток слишком много — доступ блокируется
3. Счетчик сбрасывается автоматически через указанное время
4. Frontend показывает оставшиеся попытки и таймер

---

## 🔒 Шифрование данных

### 1. Медицинские данные детей (AES-256-GCM)

**Что шифруется:**
- Диагнозы (diagnosis)
- Назначения врачей (prescription)
- Заметки и комментарии (notes)
- Имена врачей и клиник (doctor, clinic)
- Результаты анализов (result)

**Как это работает:**

```python
# ДОБАВЛЕНИЕ ДАННЫХ
Пользователь вводит: "ОРВИ, температура 38.5"
       ↓
Шифрование AES-256-GCM
       ↓
В базе сохраняется: "k8ZxP2mQ1w==:xF3kL9pM2jR8tW5nY7qS4uB6vC1hD0=="
       ↓
Расшифровка только при чтении авторизованным пользователем
       ↓
Пользователь видит: "ОРВИ, температура 38.5"
```

**Защита:**
- Ключ шифрования хранится отдельно от данных
- Каждая запись шифруется уникальным nonce (вектором инициализации)
- Невозможно расшифровать без ключа даже при доступе к базе

### 2. HTTPS для всех соединений

Все данные между браузером и сервером передаются по защищенному каналу HTTPS (TLS 1.3).

---

## 📊 Аудит и мониторинг

### 1. Журнал безопасности (Security Audit Log)

**Логируемые события:**

| Событие | Что сохраняется | Срок хранения |
|---------|-----------------|---------------|
| Вход в систему | user_id, IP, метод | 1 год |
| Регистрация | user_id, email/phone | 1 год |
| Экспорт данных | user_id, формат, количество | 1 год |
| Изменение настроек | user_id, измененные поля | 1 год |
| Неудачные попытки входа | IP, причина | 1 год |

**Пример записи:**
```json
{
  "user_id": 1,
  "action_type": "login",
  "status": "success",
  "details": {
    "phone": "+79991234567",
    "method": "password",
    "ip": "192.168.1.100"
  },
  "created_at": "2026-01-17T12:30:00Z"
}
```

### 2. Автоматическая очистка данных

**Политика хранения:**
- Сессии: удаляются через 30 дней неактивности
- Старые токены восстановления: удаляются через 24 часа
- Логи безопасности: хранятся 1 год, затем архивируются
- Удаленные аккаунты: данные удаляются безвозвратно через 7 дней

**Запуск очистки:**
```bash
# Автоматически через cron (ежедневно в 03:00)
# Или вручную (требуется ADMIN_TOKEN)
curl -X POST https://functions.poehali.dev/[data-cleanup-url] \
  -H "X-Admin-Token: YOUR_ADMIN_TOKEN"
```

---

## 📜 Политики и согласия

### 1. Обязательное согласие при регистрации

Пользователь **не может** зарегистрироваться без явного согласия:

```
☑️ Я прочитал(а) и принимаю политику конфиденциальности и условия использования
```

**Реализация:**
- Чекбокс обязателен (HTML required)
- Кнопка регистрации заблокирована до принятия
- Валидация на клиенте и сервере
- Ссылки открываются в новой вкладке для удобства

### 2. Политика конфиденциальности

**Основные разделы:**
1. Какие данные собираются
2. Как используются данные
3. Меры защиты данных (bcrypt, шифрование, аудит)
4. Права пользователей (доступ, изменение, удаление)
5. Срок хранения данных
6. Контакты для вопросов

📄 **Полный текст:** [/privacy-policy](/privacy-policy)

---

## 👨‍💻 Для разработчиков

### Архитектура безопасности

#### 1. Backend Functions (Cloud Functions)

**auth** - Аутентификация
```python
# backend/auth/index.py
from rate_limit_helper import check_rate_limit
from audit_helper import log_auth_action

def login_user(email: str, password: str, ip_address: str):
    # 1. Проверка rate limit
    if not check_rate_limit(ip_address, 'auth'):
        return {'error': 'Слишком много попыток'}
    
    # 2. Проверка bcrypt хеша
    if not bcrypt.checkpw(password.encode(), user['password_hash'].encode()):
        log_auth_action(user['id'], 'login', status='failure')
        return {'error': 'Неверный пароль'}
    
    # 3. Логирование успешного входа
    log_auth_action(user['id'], 'login', status='success')
    return {'success': True, 'token': generate_token()}
```

**children-data** - Работа с медицинскими данными
```python
# backend/children-data/index.py
from encryption_helper import encrypt_medical_fields, decrypt_medical_fields

# При добавлении данных
encrypted_data = encrypt_medical_fields({
    'diagnosis': 'ОРВИ',
    'notes': 'Температура 38.5'
})
# Сохраняем зашифрованные данные

# При чтении данных
data = fetch_from_db()
decrypted_data = decrypt_medical_fields(data)
# Возвращаем расшифрованные данные
```

**rate-limiter** - Контроль частоты запросов
```python
# backend/rate-limiter/index.py
LIMITS = {
    'auth': {'max_attempts': 5, 'window_minutes': 15},
    'password_reset': {'max_attempts': 3, 'window_minutes': 30}
}

def check_rate_limit(ip: str, action_type: str):
    # Проверка в БД: сколько попыток за период
    # Возврат: {'allowed': bool, 'remaining': int, 'reset_at': datetime}
```

#### 2. Frontend Integration

**Rate Limiting UI** (Login.tsx, Register.tsx)
```typescript
// Проверка перед попыткой входа
const checkRateLimit = async () => {
  const response = await fetch(RATE_LIMITER_URL, {
    method: 'POST',
    body: JSON.stringify({ action_type: 'auth', log_attempt: false })
  });
  const data = await response.json();
  
  setRateLimitInfo({
    remaining: data.remaining,
    blocked: !data.allowed,
    resetAt: data.reset_at
  });
};

// UI предупреждение
{rateLimitInfo.remaining <= 2 && (
  <div className="warning">
    ⚠️ Осталось попыток: {rateLimitInfo.remaining}
  </div>
)}
```

#### 3. Database Schema

**Таблицы безопасности:**
```sql
-- Журнал аудита
CREATE TABLE security_audit_log (
    id SERIAL PRIMARY KEY,
    user_id INTEGER,
    action_type VARCHAR(50),  -- 'login', 'register', 'data_export'
    status VARCHAR(20),        -- 'success', 'failure'
    ip_address VARCHAR(45),
    details JSONB,
    error_message TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Лог rate limiting
CREATE TABLE rate_limit_log (
    id SERIAL PRIMARY KEY,
    ip_address VARCHAR(45),
    action_type VARCHAR(50),   -- 'auth', 'password_reset'
    attempt_count INTEGER,
    window_start TIMESTAMP,
    last_attempt TIMESTAMP
);

-- Токены восстановления пароля
CREATE TABLE password_resets (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255),
    reset_code VARCHAR(6),
    expires_at TIMESTAMP,
    used_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### 4. Environment Variables (Secrets)

```bash
# Аутентификация
DATABASE_URL=postgresql://...
YANDEX_CLIENT_ID=...
YANDEX_CLIENT_SECRET=...

# Безопасность
ADMIN_TOKEN=...              # Для data-cleanup
ENCRYPTION_KEY=...           # Base64, 32 байта (AES-256)

# Функции
RATE_LIMITER_URL=https://functions.poehali.dev/...
AUDIT_LOGGER_URL=https://functions.poehali.dev/...
ENCRYPTION_HELPER_URL=https://functions.poehali.dev/...
```

### Checklist для новых функций

При добавлении новой функциональности с чувствительными данными:

- [ ] Добавлена проверка rate limit (если публичный endpoint)
- [ ] Добавлено логирование в audit_log (для критичных операций)
- [ ] Чувствительные поля шифруются перед сохранением
- [ ] Проверка авторизации (токен в headers)
- [ ] Обработка CORS (OPTIONS метод)
- [ ] Валидация входных данных
- [ ] Graceful degradation (fallback при ошибках внешних сервисов)
- [ ] Тесты безопасности (tests.json)

---

## 🚨 Инциденты безопасности

### Процедура реагирования

**При обнаружении уязвимости:**

1. **Немедленно**: Сообщите в security@nasha-semya.ru
2. **Укажите**:
   - Описание уязвимости
   - Шаги для воспроизведения
   - Потенциальное влияние
   - Ваши контакты для связи

3. **Мы гарантируем**:
   - Ответ в течение 24 часов
   - Исправление критичных уязвимостей в течение 48 часов
   - Публичное признание вашего вклада (по желанию)

### Что делать при подозрении на взлом аккаунта

**Признаки компрометации:**
- Неизвестные входы в систему
- Изменения данных, которые вы не делали
- Письма о восстановлении пароля, которые вы не запрашивали

**Действия:**
1. Немедленно смените пароль
2. Проверьте активные сессии (Настройки → Безопасность)
3. Завершите все сессии кроме текущей
4. Свяжитесь с поддержкой: support@nasha-semya.ru

---

## 📞 Контакты

**Вопросы по безопасности:**
- Email: security@nasha-semya.ru
- Telegram: @nasha_semya_support

**Общая поддержка:**
- Email: support@nasha-semya.ru
- Телеграм-канал: https://t.me/+QgiLIa1gFRY4Y2Iy

---

## 📚 Дополнительные ресурсы

- [152-ФЗ "О персональных данных"](https://base.garant.ru/12148567/)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [bcrypt - адаптивное хеширование](https://en.wikipedia.org/wiki/Bcrypt)
- [AES-GCM шифрование](https://en.wikipedia.org/wiki/Galois/Counter_Mode)

---

**Последнее обновление:** 17 января 2026  
**Версия документа:** 1.0.0

🛡️ **Ваши данные защищены на банковском уровне**
