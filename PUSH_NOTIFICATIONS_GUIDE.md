# 📱 Полная справка по Push-уведомлениям в проекте

## 🎯 Общая информация

В проекте используется **ОДНА система серверных push-уведомлений** для PWA:

### **Server Push-уведомления** (через бэкенд)
- 📍 Файл: `src/hooks/useNotifications.ts` + бэкенд `/backend/push-notifications`
- ⚙️ Работают: **Через сервер Cloud Functions + Web Push API + Service Worker**
- 🔔 Отправляют: Уведомления даже когда PWA закрыто
- 📲 Используются: Для всех событий (календарь, задачи, голосования, покупки, дни рождения)
- ✅ Плюсы: Работают даже когда сайт закрыт, persistent, синхронизация между устройствами
- 🚀 Идеально для PWA: Service Worker, подписка через бэкенд, VAPID ключи

---

## 📡 Server Push-уведомления (useNotifications)

### 📂 Где используются

#### ✅ **Календарь** (`src/pages/Calendar.tsx`)
```typescript
const { notifyCalendarEvent } = useNotifications();
// При создании/редактировании события
notifyCalendarEvent(eventData.title, new Date(eventData.date).toLocaleDateString('ru-RU'), false);
```

#### ✅ **Задачи** (`src/components/tabs/TasksTabContent.tsx`)
```typescript
const { notifyTaskAssigned } = useNotifications();
// При создании задачи
if (assigneeMember) {
  notifyTaskAssigned(result.task.title, assigneeMember.name, result.task.id);
}
```

#### ✅ **Голосования** (`src/components/VotingWidget.tsx`)
```typescript
const { notifyVotingCreated } = useNotifications();
// При создании голосования
notifyVotingCreated(result.voting.title, result.voting.id);
```

#### ✅ **Покупки** (`src/pages/Shopping.tsx`)
```typescript
const { notifyUrgentShopping } = useNotifications();
// При добавлении срочной покупки
if (newItem.priority === 'urgent') {
  notifyUrgentShopping(newItem.name);
}
```

#### ✅ **Дни рождения** (`src/hooks/useBirthdayReminders.ts`)
```typescript
const { notifyBirthday } = useNotifications();
// Автоматически проверяется каждый час
// За день: notifyBirthday(member.name, true);
// В день: notifyBirthday(member.name, false);
```

#### ⏳ **Где ещё можно добавить** (функции готовы, но не используются):

1. **Лекарства** (`/health` или `/children`) - напоминания о приёме
```typescript
const { notifyMedicationReminder } = useNotifications();
notifyMedicationReminder(medicationName, childName, time);
```

2. **Подписка** (`/settings`) - когда подписка истекает
```typescript
const { notifySubscriptionExpiring } = useNotifications();
notifySubscriptionExpiring(daysLeft);
```

---

### 🔧 Доступные функции

```typescript
const {
  sendNotification,           // Универсальная отправка
  notifyVotingCreated,        // Голосование создано
  notifyUrgentShopping,       // Срочная покупка
  notifyTaskAssigned,         // Задача назначена
  notifyCalendarEvent,        // Событие в календаре
  notifySubscriptionExpiring, // Подписка истекает
  notifyMedicationReminder,   // Напоминание о лекарстве
  notifyBirthday,            // День рождения
} = useNotifications();
```

---

## 🎯 Где можно добавить уведомления

### 🟢 Готовы к использованию (нужно только интегрировать):

1. **Задачи** (`src/pages/Tasks.tsx` или где создаются задачи)
   - При создании задачи → `notifyTaskAssigned()`
   - При изменении статуса → новая функция
   - При приближении дедлайна → новая функция

2. **Голосования** (`src/pages/VotingPage.tsx`)
   - При создании голосования → `notifyVotingCreated()`
   - При окончании голосования → новая функция

3. **Покупки** (`src/pages/Shopping.tsx`)
   - При добавлении срочной покупки → `notifyUrgentShopping()`
   - Когда товар куплен → новая функция

4. **Здоровье/Дети** (`src/pages/Health.tsx` или `src/pages/Children.tsx`)
   - Напоминания о лекарствах → `notifyMedicationReminder()`
   - Приёмы врача → использовать календарь
   - Прививки → использовать календарь

5. **Дни рождения** (`src/pages/Tree.tsx` или семейное дерево)
   - За день до дня рождения → `notifyBirthday(name, true)`
   - В день рождения → `notifyBirthday(name, false)`

6. **Подписка** (`src/pages/Settings.tsx`)
   - За 7/3/1 день до окончания → `notifySubscriptionExpiring(daysLeft)`

### 🔵 Новые функции (нужно добавить):

1. **Поездки** (`src/pages/Trips.tsx`)
   - За день до поездки
   - Время выезда

2. **Семейные цели** (`src/pages/Index.tsx`)
   - Когда цель достигнута
   - Когда прогресс застопорился

3. **Обучение** (`src/pages/Education.tsx`)
   - Домашние задания
   - Экзамены

4. **Питание** (`src/pages/Meals.tsx`)
   - Пора готовить ужин
   - Продукты заканчиваются

---

## ⚙️ Настройки уведомлений

### 📍 Где находятся:
- **Компонент**: `src/components/NotificationsSettings.tsx`
- **Страница**: `/settings` → раздел "Уведомления"
- **Хук**: `src/hooks/usePushNotifications.ts`

### 🎛️ Что можно настроить:
1. **Включить/отключить** все push-уведомления
2. **Типы уведомлений** (календарь, задачи, покупки и т.д.)
3. **Тестовое уведомление** - проверить работу
4. **Проверка запланированных** - запустить отправку вручную
5. **Переподписка** - обновить VAPID ключи

---

## 🚀 Рекомендации по внедрению

### Приоритет 1 (срочно добавить):
1. ✅ **Календарь** - уже работает
2. 🟡 **Задачи** - `notifyTaskAssigned()` готова, нужно вызвать
3. 🟡 **Дни рождения** - `notifyBirthday()` готова, нужно вызвать

### Приоритет 2 (желательно):
4. 🟡 **Голосования** - `notifyVotingCreated()` готова
5. 🟡 **Покупки срочные** - `notifyUrgentShopping()` готова
6. 🟡 **Лекарства** - `notifyMedicationReminder()` готова

### Приоритет 3 (по желанию):
7. 🟢 **Подписка истекает** - `notifySubscriptionExpiring()` готова
8. 🔵 **Поездки** - нужна новая функция
9. 🔵 **Цели** - нужна новая функция

---

## 🛠️ Техническая информация

### Браузерная система:
- **Хранилище**: localStorage (`scheduledNotifications`)
- **Таймеры**: setTimeout (сбрасываются при перезагрузке, но восстанавливаются)
- **Ограничения**: Максимальная задержка setTimeout ~24 дня
- **Работает в**: Chrome, Firefox, Edge, Safari (в PWA)

### Серверная система:
- **Бэкенд**: `/backend/push-notifications/index.py`
- **Service Worker**: `/public/sw.js`
- **Подписки**: Хранятся на сервере в PostgreSQL
- **VAPID ключи**: В секретах проекта
- **Работает в**: Chrome, Firefox, Edge, Safari PWA (iOS 16.4+)

### Поддержка браузеров:

| Браузер | Локальные уведомления | Server Push | 
|---------|----------------------|-------------|
| Chrome Desktop | ✅ | ✅ |
| Firefox Desktop | ✅ | ✅ |
| Edge | ✅ | ✅ |
| Safari Desktop | ✅ | ❌ |
| Chrome Android | ✅ | ✅ |
| Safari iOS (браузер) | ❌ | ❌ |
| Safari iOS (PWA) | ✅ | ✅ (iOS 16.4+) |

---

## 📝 Итоги

### ✅ Что уже работает:
- Серверные push-уведомления через Service Worker
- Календарь — уведомления о событиях
- Задачи — когда задача назначена
- Голосования — при создании голосования
- Покупки — срочные покупки
- Дни рождения — автоматически за день и в день рождения

### 🟡 Что готово к использованию (функции есть, нужна интеграция):
- Лекарства — `notifyMedicationReminder()`
- Подписка — `notifySubscriptionExpiring()`

### 🔵 Что нужно создать (новые функции):
- Поездки, Цели семьи, Обучение, Питание  

---

**Создано**: 2026-01-22  
**Версия**: 1.0  
**Автор**: Юра 🚀