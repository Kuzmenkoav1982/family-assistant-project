# 🔔 Система автоматических напоминаний

## Описание

Система автоматических push-уведомлений для напоминаний о задачах, событиях и важных датах в семейном органайзере.

## Возможности

### 📋 Автоматические напоминания:
- **Задачи** - уведомления за день до срока выполнения
- **События** - напоминания о предстоящих мероприятиях в календаре
- **Лекарства** - напоминания о приёме медикаментов детям
- **Важные даты** - дни рождения и годовщины членов семьи

### ⚡ Технические особенности:
- PWA (Progressive Web App) с Service Worker
- Push API для браузеров (Chrome, Firefox, Edge, Safari 16.4+)
- Работает без интернета (офлайн-режим)
- Автоматическая отправка каждый день в 9:00

## Архитектура

### Backend функции:

1. **`/backend/push-notifications/`** - Управление подписками
   - POST `/subscribe` - подписка на уведомления
   - POST `/send` - отправка тестового уведомления
   - DELETE - отписка от уведомлений
   - GET - проверка статуса подписки

2. **`/backend/notification-scheduler/`** - Автоматический планировщик
   - Проверяет задачи, события, лекарства
   - Формирует и отправляет уведомления
   - Запускается по расписанию (cron job)

### Frontend компоненты:

- **`/src/hooks/usePushNotifications.ts`** - React hook для управления подписками
- **`/src/components/NotificationsSettings.tsx`** - UI настроек уведомлений
- **`/src/components/PWAInstallPrompt.tsx`** - Подсказка установки PWA
- **`/public/sw.js`** - Service Worker для обработки push-событий

### База данных:

Таблица `push_subscriptions`:
```sql
CREATE TABLE push_subscriptions (
    id SERIAL PRIMARY KEY,
    family_id VARCHAR(255) NOT NULL UNIQUE,
    subscription_data JSONB NOT NULL,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

## Установка и настройка

### Для пользователей:

1. Откройте приложение в браузере (Chrome/Firefox/Edge/Safari)
2. Разрешите уведомления при первом входе
3. Перейдите в **Настройки** → **Уведомления**
4. Включите переключатель **Push-уведомления**
5. Нажмите **Отправить тестовое уведомление** для проверки

### Для разработчиков:

#### 1. Установка PWA на устройство:

**iOS (Safari):**
- Поделиться → На экран "Домой" → Добавить

**Android (Chrome):**
- Меню (⋮) → Установить приложение

#### 2. Настройка автоматического планировщика:

Функция `/backend/notification-scheduler/` должна запускаться автоматически каждый день.

**Вариант 1: Cron job на сервере**
```bash
# Запуск каждый день в 9:00
0 9 * * * curl -X GET https://functions.poehali.dev/1f91cf2c-e194-4bc4-a216-531a511d6b16
```

**Вариант 2: Cloud Functions Trigger (Yandex Cloud)**
```yaml
triggers:
  - name: daily-notifications
    rule:
      type: timer
      schedule: '0 9 * * ? *'
    function:
      id: notification-scheduler
```

**Вариант 3: GitHub Actions**
```yaml
name: Daily Notifications
on:
  schedule:
    - cron: '0 9 * * *'
jobs:
  send-notifications:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger notifications
        run: curl -X GET https://functions.poehali.dev/1f91cf2c-e194-4bc4-a216-531a511d6b16
```

## API документация

### Push Notifications API

**URL:** `https://functions.poehali.dev/3c808a69-0f14-4db0-b486-3e2a0e273a94`

#### Подписка на уведомления
```http
POST /
Content-Type: application/json
X-Auth-Token: <user_token>

{
  "action": "subscribe",
  "subscription": {
    "endpoint": "https://...",
    "keys": {
      "p256dh": "...",
      "auth": "..."
    }
  }
}
```

#### Отправка тестового уведомления
```http
POST /
Content-Type: application/json
X-Auth-Token: <user_token>

{
  "action": "send",
  "title": "Тест",
  "message": "Проверка уведомлений"
}
```

#### Отписка
```http
DELETE /
X-Auth-Token: <user_token>
```

### Notification Scheduler API

**URL:** `https://functions.poehali.dev/1f91cf2c-e194-4bc4-a216-531a511d6b16`

#### Запуск планировщика
```http
GET /
```

**Ответ:**
```json
{
  "success": true,
  "notifications_sent": 15,
  "families_checked": 8,
  "timestamp": "2025-12-01T09:00:00"
}
```

## Логика работы планировщика

```python
# Для каждой семьи с подпиской:
1. Проверить задачи на завтра (due_date = tomorrow)
   → Если найдены: отправить уведомление "Завтра N задач"
   
2. Проверить события на завтра (event_date = tomorrow)
   → Если найдены: отправить уведомление "Завтра N событий"
   
3. Проверить активные лекарства (start_date <= today <= end_date)
   → Если найдены: отправить уведомление "Напоминание о лекарствах"
```

## Примеры уведомлений

### Задачи:
```
📋 Завтра 3 задачи
Напоминаем: Купить продукты, Забрать посылку, Записаться к врачу
```

### События:
```
📅 Завтра 2 события
Не пропустите: День рождения мамы, Родительское собрание
```

### Лекарства:
```
💊 Напоминание о лекарствах
Не забудьте дать лекарства: Маша и Петя
```

## Troubleshooting

### Уведомления не приходят:

1. **Проверьте разрешения браузера:**
   - Chrome: Настройки → Конфиденциальность и безопасность → Настройки сайтов → Уведомления
   - Firefox: Настройки → Приватность и защита → Разрешения → Уведомления
   - Safari: Настройки → Веб-сайты → Уведомления

2. **Проверьте статус подписки:**
   - Перейдите в Настройки → Уведомления
   - Должен быть включен переключатель и статус "Разрешены"

3. **Проверьте работу планировщика:**
   ```bash
   curl https://functions.poehali.dev/1f91cf2c-e194-4bc4-a216-531a511d6b16
   ```

4. **Проверьте логи в браузере:**
   - F12 → Console → Ищите ошибки push/notification

### iOS Safari не показывает уведомления:

- Убедитесь, что версия Safari >= 16.4
- Проверьте, что сайт добавлен на главный экран (PWA)
- Перейдите в Настройки iOS → Уведомления → Safari

## Безопасность

- Все подписки привязаны к `family_id` (изоляция данных)
- Требуется авторизация через `X-Auth-Token`
- VAPID ключи для защиты от подделки уведомлений
- HTTPS обязателен для работы Push API

## Метрики и мониторинг

Рекомендуется отслеживать:
- Количество активных подписок
- Процент успешных отправок
- Количество уведомлений в день
- Время отклика планировщика

## Roadmap

- [ ] Настройка времени отправки уведомлений
- [ ] Выбор типов уведомлений (задачи/события/лекарства)
- [ ] Групповые уведомления (объединение нескольких событий)
- [ ] Rich notifications (с кнопками действий)
- [ ] Статистика открытий уведомлений

## Поддержка браузеров

| Браузер | Версия | Поддержка |
|---------|--------|-----------|
| Chrome | 42+ | ✅ Полная |
| Firefox | 44+ | ✅ Полная |
| Edge | 17+ | ✅ Полная |
| Safari | 16.4+ | ✅ Полная |
| Opera | 37+ | ✅ Полная |
| Samsung Internet | 5+ | ✅ Полная |

## Лицензия

MIT License - см. LICENSE файл

---

**Автор:** Семейный Ассистент Pro  
**Версия:** 1.0.0  
**Дата:** Декабрь 2025
