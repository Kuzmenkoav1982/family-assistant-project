# 📧 Настройка Email через Yandex Cloud Postbox

Инструкция по подключению реальной отправки email для восстановления пароля.

## Шаг 1: Настройка Yandex Cloud Postbox

1. Перейдите в [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Выберите ваш фолдер
3. Перейдите в **Postbox** (в разделе "Сервисы")
4. Нажмите **Создать адрес**
5. Укажите:
   - Домен (ваш собственный домен)
   - Адрес отправителя (например: `noreply@yourdomain.ru`)

## Шаг 2: Подтверждение домена

1. Добавьте DNS-записи для подтверждения домена:
   - TXT-запись для SPF
   - TXT-запись для DKIM
   - MX-запись (опционально)

2. Дождитесь подтверждения (обычно 10-30 минут)

## Шаг 3: Получение SMTP-данных

После подтверждения домена вы получите:
- **SMTP Host**: `smtp.yandex.ru`
- **SMTP Port**: `465` (SSL) или `587` (TLS)
- **SMTP User**: ваш email (например: `noreply@yourdomain.ru`)
- **SMTP Password**: пароль от Postbox (создается в консоли)

## Шаг 4: Добавление секретов в проект

В интерфейсе poehali.dev добавьте следующие секреты:

### 1. SMTP_HOST
**Значение**: `smtp.yandex.ru`

### 2. SMTP_PORT
**Значение**: `465` (для SSL) или `587` (для TLS)

### 3. SMTP_USER
**Значение**: ваш email (например: `noreply@yourdomain.ru`)

### 4. SMTP_PASSWORD
**Значение**: пароль от Postbox (из консоли Yandex Cloud)

### 5. SMTP_FROM_NAME
**Значение**: `Наша семья` (или любое другое имя отправителя)

## Шаг 5: Проверка работы

1. Перейдите на страницу восстановления пароля
2. Введите email, зарегистрированный в системе
3. Нажмите "Отправить код"
4. Проверьте почту - должно прийти красивое письмо с кодом

## Формат письма

Письмо приходит в виде:
- 🎨 Красивый дизайн с градиентом
- 🔢 Крупный 6-значный код
- ⏰ Указание срока действия (15 минут)
- 💼 Профессиональное оформление

## Альтернативные варианты

### Яндекс.Почта для домена (бесплатно до 1000 писем/день)
- [Подключить](https://domain.yandex.ru/domains_add/)
- Те же SMTP-настройки

### Другие SMTP-провайдеры
Можно использовать любой SMTP-сервис:
- Gmail SMTP
- SendGrid
- Mailgun
- AWS SES

Просто измените соответствующие секреты.

## Troubleshooting

### Письма не приходят
1. Проверьте, что домен подтвержден в Yandex Cloud
2. Проверьте правильность SMTP-данных
3. Проверьте папку "Спам"
4. Проверьте логи функции через get_logs

### Ошибка аутентификации
1. Убедитесь, что пароль SMTP скопирован без пробелов
2. Проверьте, что используете правильный порт (465 для SSL)
3. Попробуйте пересоздать пароль в Yandex Cloud Console

### DNS не подтверждается
1. Подождите до 48 часов (обычно быстрее)
2. Проверьте правильность DNS-записей через dig/nslookup
3. Убедитесь, что записи добавлены у вашего регистратора домена

## Безопасность

✅ **Что защищено**:
- Пароли хранятся как секреты (не в коде)
- Используется SSL/TLS для отправки
- Код действует только 15 минут
- После смены пароля все сессии сбрасываются

⚠️ **Рекомендации**:
- Используйте отдельный email для отправки (не личный)
- Регулярно меняйте SMTP-пароль
- Мониторьте количество отправленных писем
- Настройте лимиты в Yandex Cloud

## Стоимость

**Yandex Cloud Postbox**:
- Первые 10,000 писем/месяц: бесплатно
- Далее: ~0.60₽ за письмо

Для семейного приложения обычно хватает бесплатного лимита.
