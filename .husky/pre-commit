#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Проверка актуальности функций перед коммитом
echo "🔍 Проверка актуальности backend функций..."
bun run scripts/check-functions.ts

if [ $? -ne 0 ]; then
  echo "❌ Найдены устаревшие URL функций. Коммит отменён."
  exit 1
fi

echo "✅ Все функции актуальны"
