# Умные виджеты членов семьи — Документация

## 🎯 Обзор

Обновлённые виджеты членов семьи теперь отображают реальную загруженность и полезные метрики вместо статичной информации (возраст, роль).

## 📊 Новые возможности

### 1. **Прогресс-бар загруженности**
- **Зелёный (0-50%)** — Свободен
- **Жёлтый (50-80%)** — Загружен
- **Красный (80-100%)** — Перегружен

**Расчёт загруженности:**
```
Загруженность = (активные задачи × 20) + (обязанности × 10) + (события сегодня × 15)
Максимум: 195 баллов → нормализовано к 100%
```

### 2. **Мини-метрики**
- 📋 **Активные задачи** — сколько задач в работе
- ✅ **Завершено сегодня** — продуктивность за день
- 📅 **События на сегодня** — запланированные мероприятия
- 🏆 **Достижения за неделю** — новые награды (последние 7 дней)

### 3. **Быстрые действия**
- Кнопка **"+ Задача"** — мгновенное назначение задания
- Клик на виджет — переход в полный профиль

### 4. **Настройка виджетов**
Кнопка **"Настроить виджеты"** позволяет:
- Выбрать пресеты: Минималист / Стандарт / Подробный
- Включить/выключить отдельные метрики
- Показать/скрыть возраст и роль
- Управлять быстрыми действиями

## 📁 Структура файлов

### Новые файлы:
```
src/
├── utils/
│   └── memberWorkload.ts          # Расчёт загруженности
├── types/
│   └── widgetSettings.ts          # Типы и хранение настроек
├── components/
│   ├── WidgetSettingsDialog.tsx   # Диалог настройки
│   └── FamilyProfilesInstructions.tsx  # Большая инструкция
```

### Обновлённые файлы:
```
src/
├── components/
│   ├── FamilyMembersGrid.tsx      # Обновлён MemberCard
│   └── index/MainContent.tsx      # Добавлена кнопка настройки
├── types/
│   └── family.types.ts            # Добавлен moodStatus
```

## 🛠️ API

### `calculateMemberWorkload(member, tasks, events)`
Возвращает метрики загруженности:
```typescript
interface WorkloadMetrics {
  activeTasks: number;
  completedToday: number;
  responsibilities: number;
  todayEvents: number;
  weekAchievements: number;
  workloadPercentage: number;
  workloadStatus: 'free' | 'busy' | 'overloaded';
  workloadLabel: string;
  workloadColor: string;
}
```

### `loadWidgetSettings()` / `saveWidgetSettings(settings)`
Управление настройками виджета в localStorage.

## 🎨 Пресеты настроек

### Минималист
- Только загруженность и активные задачи
- Компактный размер
- Без дополнительных действий

### Стандарт (по умолчанию)
- Загруженность
- Активные задачи, завершено сегодня, события, достижения
- Быстрые действия

### Подробный
- Всё включено: возраст, роль, все метрики
- Детальный размер

## 💾 Хранение данных

Настройки виджетов сохраняются в `localStorage`:
```typescript
{
  showAge: false,
  showRole: false,
  showWorkload: true,
  showActiveTasks: true,
  showCompletedToday: true,
  showResponsibilities: false,
  showTodayEvents: true,
  showWeekAchievements: true,
  showQuickActions: true,
  widgetSize: 'normal'
}
```

## 🔄 Автоматическое обновление

Метрики обновляются автоматически при:
- Изменении задач (создание, завершение, удаление)
- Добавлении событий в календарь
- Обновлении обязанностей члена семьи
- Получении новых достижений

## 📱 Адаптивность

- **Мобильные** — компактное отображение, сетка 2 колонки
- **Планшет** — 2 колонки
- **Десктоп** — 3 колонки с детальной информацией

## 🎯 Примеры использования

### Распределение задач
```typescript
// Найти наименее загруженного члена семьи
const leastBusy = members
  .map(m => ({ 
    member: m, 
    metrics: calculateMemberWorkload(m, tasks, events) 
  }))
  .sort((a, b) => a.metrics.workloadPercentage - b.metrics.workloadPercentage)[0];

// Назначить задачу
onAssignTask(leastBusy.member.id);
```

### Мотивация
```typescript
// Найти самого продуктивного сегодня
const mostProductive = members
  .map(m => ({ 
    member: m, 
    metrics: calculateMemberWorkload(m, tasks, events) 
  }))
  .sort((a, b) => b.metrics.completedToday - a.metrics.completedToday)[0];

// Показать похвалу
toast.success(`${mostProductive.member.name} выполнил ${mostProductive.metrics.completedToday} задач сегодня! 🎉`);
```

## 🐛 Известные ограничения

1. Загруженность не учитывает **время выполнения** задач (только количество)
2. Достижения за неделю работают только если в строке есть дата в формате `YYYY-MM-DD`
3. События учитываются только если указаны `participants`

## 🚀 Будущие улучшения

- [ ] Учёт времени выполнения задач при расчёте загруженности
- [ ] История изменения загруженности (графики)
- [ ] Предсказание перегрузки на основе трендов
- [ ] Умные рекомендации по распределению задач
- [ ] Push-уведомления при перегрузке
- [ ] Экспорт статистики загруженности в CSV
