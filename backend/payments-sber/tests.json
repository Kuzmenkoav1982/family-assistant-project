{
  "tests": [
    {
      "name": "Получение вариантов донатов (публичный endpoint)",
      "method": "GET",
      "path": "/?action=presets",
      "expectedStatus": 200,
      "expectedBody": {
        "presets": "array",
        "custom_allowed": true
      },
      "bodyMatcher": "partial"
    },
    {
      "name": "Получение статистики донатов (публичный endpoint)",
      "method": "GET",
      "path": "/?action=stats",
      "expectedStatus": 200,
      "expectedBody": {
        "total_donations": "number"
      },
      "bodyMatcher": "partial"
    },
    {
      "name": "OPTIONS preflight для CORS",
      "method": "OPTIONS",
      "path": "/",
      "expectedStatus": 200
    }
  ]
}