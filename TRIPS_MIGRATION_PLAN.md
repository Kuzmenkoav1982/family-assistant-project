# 🛫 План миграции раздела "Путешествия"

## Текущая ситуация

**ПРОБЛЕМА:** Путешествия находятся в двух местах одновременно:
1. ✅ `/trips` — новый работающий раздел (в списке "В разработке")
2. ⚠️ `/travel` — старая страница (возможно дубликат)

**ЦЕЛЬ:** Оставить один рабочий раздел `/trips` и удалить дубликаты.

---

## Шаг 1: Проверка старого раздела `/travel`

**Действие:** Открыть `/travel` и посмотреть что там

**Вопросы:**
- [ ] Есть ли в `/travel` функционал, которого нет в `/trips`?
- [ ] Используется ли `/travel` где-то в коде?
- [ ] Можно ли удалить `/travel` без потерь?

**Результат:** Если `/travel` пустой или дубликат → удаляем.

---

## Шаг 2: Перенос `/trips` из "В разработке" в основное меню

### Изменения в `src/pages/Index.tsx`:

#### 1. Убираем из `inDevelopmentSections` (строки 795-809):

```typescript
// УДАЛЯЕМ ЭТО:
{ 
  id: 'travel', 
  icon: 'Plane', 
  label: 'Путешествия',
  description: 'Планирование поездок и воспоминания',
  features: [...],
  onClick: () => navigate('/trips')
},
```

#### 2. Добавляем в `availableSections` (строка 573):

```typescript
const availableSections = [
  { id: 'family', icon: 'Users', label: 'Профили семьи' },
  { id: 'tasks', icon: 'CheckSquare', label: 'Задачи' },
  { id: 'recipes', icon: 'ChefHat', label: 'Рецепты' },
  { id: 'trips', icon: 'Plane', label: 'Путешествия' }, // ← ДОБАВИТЬ
  { id: 'analytics', icon: 'BarChart3', label: 'Аналитика' },
  { id: 'calendar', icon: 'Calendar', label: 'Календарь' },
  // ... остальное
];
```

#### 3. Добавляем обработчик клика в `LeftMenu.tsx` (строка 173):

```typescript
onClick={() => {
  console.log('Left menu clicked:', section.id);
  if (section.id === 'trips') {
    navigate('/trips'); // ← ДОБАВИТЬ
  } else if (section.id === 'psychologist') {
    navigate('/psychologist');
  }
  // ...
}}
```

---

## Шаг 3: Опционально — добавить бейдж "Beta"

Если хотите показать что раздел ещё дорабатывается:

```typescript
const availableSections = [
  { id: 'trips', icon: 'Plane', label: 'Путешествия', badge: 'Beta' },
];
```

И в `LeftMenu.tsx`:

```tsx
<span className="text-sm font-medium">{section.label}</span>
{section.badge && (
  <Badge variant="outline" className="ml-auto text-xs">{section.badge}</Badge>
)}
```

---

## Шаг 4: Удаление старого `/travel` (если это дубликат)

### 1. Удалить файл:
```bash
rm src/pages/Travel.tsx
```

### 2. Убрать из `App.tsx`:
```typescript
// УДАЛИТЬ ЭТУ СТРОКУ:
import Travel from "./pages/Travel";

// И УДАЛИТЬ РОУТ:
<Route path="/travel" element={<Travel />} />
```

---

## Итоговая структура

После изменений:

✅ `/trips` — основной раздел путешествий (в левом меню)
✅ `/trips/:id` — детали поездки
✅ `/trips/wishlist` — список желаний
❌ `/travel` — удалён (если дубликат)

---

## Контрольный чеклист

- [ ] Открыл `/travel` и проверил содержимое
- [ ] Решил: удалять `/travel` или оставить
- [ ] Убрал `trips` из `inDevelopmentSections`
- [ ] Добавил `trips` в `availableSections`
- [ ] Добавил обработчик клика в `LeftMenu.tsx`
- [ ] (Опционально) Добавил бейдж "Beta"
- [ ] Удалил старый файл `Travel.tsx`
- [ ] Убрал роут `/travel` из `App.tsx`
- [ ] Протестировал: кнопка в меню открывает `/trips`
- [ ] Проверил что все ссылки работают
