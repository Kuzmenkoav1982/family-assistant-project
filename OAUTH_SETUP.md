# 🔐 OAuth Авторизация с Yandex ID

## ✅ Что уже готово

1. **Backend функция авторизации** 
   - URL: `https://functions.poehali.dev/b9b956c8-e2a6-4c20-aef8-b8422e8cb3b0`
   - Поддержка OAuth через Yandex ID
   - Автоматическое создание пользователя и семьи

2. **Frontend интеграция**
   - Страница логина `/login`
   - Кнопка "Войти через Яндекс ID" в шапке
   - Отображение данных пользователя в настройках
   - OAuth бейдж на главной странице

3. **База данных**
   - Таблица `users` с полями для OAuth
   - Автоматическое создание семьи для нового пользователя

## 🎯 Как работает OAuth flow

```
1. Пользователь → Кликает "Войти через Яндекс ID"
2. Frontend → Редирект на backend: 
   GET /auth?oauth=yandex&callback_url=...
3. Backend → Редирект на Yandex OAuth
4. Yandex → Пользователь авторизуется
5. Yandex → Редирект на callback с code
6. Backend → Обменивает code на access_token
7. Backend → Получает данные пользователя от Yandex
8. Backend → Создает/находит user, создает семью, генерирует session token
9. Backend → Редирект на frontend с token и user data
10. Frontend → Сохраняет token и user в localStorage
11. Frontend → Редирект на главную страницу
```

## 🔧 Настройки Yandex OAuth

**Client ID**: `7e0f89d2248a4f6cbecc79103c2553b8`  
**Callback URL**: `https://functions.poehali.dev/b9b956c8-e2a6-4c20-aef8-b8422e8cb3b0?oauth=yandex_callback`

## 📋 Endpoints

### 1. Инициализация OAuth
```
GET /auth?oauth=yandex&callback_url=CALLBACK_URL
```
Редиректит пользователя на Yandex OAuth

### 2. Обработка callback от Yandex
```
GET /auth?oauth=yandex_callback&code=CODE&redirect_uri=REDIRECT_URI&frontend_url=FRONTEND_URL
```
Обменивает code на токен, создает пользователя, редиректит на frontend

### 3. Получение текущего пользователя
```
GET /auth
Headers: X-Auth-Token: TOKEN
```
Возвращает данные авторизованного пользователя

## 🎨 Frontend компоненты

### Страница логина
`/login` - страница с кнопкой "Войти через Яндекс ID"

### Кнопка входа в шапке
Яркая кнопка с градиентом в верхней панели

### OAuth бейдж
Отображается рядом с названием проекта когда пользователь авторизован

### Данные пользователя в настройках
В разделе "Аккаунт" показывается:
- Аватар пользователя
- Имя
- Email
- Название семьи

## 🔑 LocalStorage

После успешной авторизации:

```javascript
localStorage.setItem('authToken', 'TOKEN');
localStorage.setItem('user', JSON.stringify({
  id: 'uuid',
  email: 'user@example.com',
  oauth_provider: 'yandex',
  name: 'Иван Иванов',
  avatar_url: 'https://...',
  family_id: 'uuid',
  family_name: 'Семья Ивановых',
  member_id: 'uuid'
}));
```

## 🧪 Тестирование

1. Перейти на `/login`
2. Нажать "Войти через Яндекс ID"
3. Авторизоваться на Yandex
4. Проверить редирект на главную страницу
5. Проверить отображение OAuth бейджа
6. Зайти в настройки → Аккаунт
7. Проверить данные пользователя

## 🚀 Следующие шаги

### Интеграция с family_members
Сейчас OAuth создает:
- User в таблице `users`
- Family в таблице `families`
- FamilyMember в таблице `family_members`

Нужно связать авторизованного пользователя с функционалом приложения:
- Отображать реальные задачи пользователя
- Показывать членов его семьи
- Фильтровать данные по family_id

### Приглашения
Реализовать функционал приглашения в семью:
- Пользователь создает invite_code
- Отправляет код родственникам
- Родственники регистрируются по коду
- Автоматически добавляются в семью

### Безопасность
- [ ] Добавить CSRF защиту
- [ ] Валидация state параметра в OAuth
- [ ] Rate limiting на авторизацию
- [ ] Логирование попыток входа

## 📚 Документация Yandex OAuth

https://yandex.ru/dev/id/doc/ru/

## 🆘 Troubleshooting

### Ошибка: "Invalid callback URL"
Проверьте, что Callback URL в настройках Yandex OAuth совпадает с тем, что отправляет backend

### Пользователь не авторизован
Проверьте localStorage на наличие authToken и user

### Ошибка при получении данных
Проверьте что X-Auth-Token передается в заголовках запросов
